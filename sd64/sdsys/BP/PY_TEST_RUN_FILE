program py_test_run_file
*
*
* must add sdsys/TEST/myscript.py
**************************************************************************
*import sys
*print (sys.path)
*FMRK  = chr(254)
*VMRKM  = chr(253)
*SVMRK  = chr(252)
*SP = chr(32)
*xyz = "XYz"
*result = ""
*countdown = {"3" : "Three", "2" : "Two", "1" : "One", "0" : "Ignition!"}
*for count in countdown:
*  result += count + SP + countdown[count] + FMRK 
*print ("result: ",result)
*result = bytes(result, 'latin')
*print ("result in bytes:")
*for byte in result:
*  print(byte, end=' ')
*print()  
*print('done')
*
*****************************************************************************
print 'embedded python test, hit <enter> to start':
input ok
script_file = "@sdsys:/TEST/myscript.py"
print 'attempt to run script: ' : script_file
print str('=',50)
*
*
print 'attempt init'
call !PY_INITIALIZE(rtn)
print 'status = ':rtn
print str('=',50)

call !PY_RUNFILE(script_file,rtn)
print 'status = ':rtn
if rtn <> 0 then goto 999

print str('=',50)
print 'attempt to access python object: result'
call !PY_GETATTR("result",rtn_string,error)
print "result:"
print rtn_string
print 'status = ':error

print str('=',50)
print 'attempt to access python object: countdown'
call !PY_GETATTR("countdown",rtn_string,error)
print "countdown:"
print rtn_string
print 'status = ':error
*
print str('=',50)
*
999: print 'attemp finalize'
call !PY_FINALIZE(rtn)
print 'status =':rtn
end
